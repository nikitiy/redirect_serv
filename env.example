# === Database settings ===
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=backoffice
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# === URLs ===
GUEST_SERV_DOMAIN=localhost

USE_HTTPS=true

# === CORS ===
CORS_ENABLED=true
CORS_ALLOW_ORIGINS=*
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=*
CORS_ALLOW_HEADERS=*
